import { VoteColorPipe } from './vote-color.pipe';

describe('VoteColorPipe', () => {
  it('create an instance', () => {
    const pipe = new VoteColorPipe();
    expect(pipe).toBeTruthy();
  });
});
